// iostream contains the definitions needed to use std::cout
#include <iostream>

// The program starts on the first line of the main() function
int main()
{
    // Print some text to the user using the std::cout stream.
    std::cout << "Hello, World!" << std::endl;

    // 0 means success. Nothing went wrong here so 0 it is.
    return 0;
}
